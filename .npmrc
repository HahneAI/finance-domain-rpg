# Required because vite-plugin-pwa@1.2.0 declares peer vite@"^3-7" but the project
# uses vite@8. The plugin works fine at runtime — this flag suppresses the resolver
# error. Remove once vite-plugin-pwa cuts a release that includes vite@8 in its
# peer dep range.
legacy-peer-deps=true
