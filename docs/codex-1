{
  "task_id": "FEATURE_budgetpanel_remove_goals_with_safe_extraction_and_wiring_guard",
  "task_type": "feature",

  "environment_domain": "authority-finance-ui",
  "allowed_environment_domains": ["authority-finance-ui"],
  "environment_reason": "This task removes Goals UI and related interaction logic from BudgetPanel and prepares for a standalone Goals surface, while requiring careful inspection of goal-related dependencies to avoid breaking data flow.",
  "environment_policy": "scope_enforced",
  "environment_runtime_visibility_policy": "report_if_available_do_not_fail_if_missing",

  "scope": {
    "allowed_files": [
      "src/components/BudgetPanel.jsx",
      "goal-related UI blocks within BudgetPanel",
      "imports used exclusively by goal UI",
      "view/tab routing inside BudgetPanel",
      "read-only selectors/props referencing goals"
    ],
    "restricted_files": [
      "computeGoalTimeline",
      "futureWeekNets",
      "computeNet",
      "core goal math helpers",
      "Supabase/data persistence",
      "other panels (GoalsPanel, IncomePanel, etc.)",
      "database/schema"
    ]
  },

  "constraints": [
    "This is a UI removal/extraction task, not a logic rewrite",
    "Do not modify core goal math functions",
    "Do not delete logic that may still be required by other parts of the app",
    "Only remove code that is strictly tied to the BudgetPanel goals UI",
    "If a function/import is potentially shared, verify usage before removal",
    "Preserve any required data flow via props/selectors (read-only allowed)",
    "No duplication of goal logic elsewhere"
  ],

  "implementation_steps": [
    "Inspect BudgetPanel.jsx and identify all goal-related props, state, functions, effects, JSX blocks, and imports.",
    
    "A. Props cleanup:",
    "Remove props: setGoals, logNetLost, logNetGained, timelineWeekNets, futureEventDeductions.",
    "Retain goals as read-only ONLY if still referenced by overview or breakdown. Verify before removal.",
    
    "B. State cleanup:",
    "Delete goal-specific state entries including editGoalId, editGoalVals, addingGoal, newGoal, delGoalId, fundingId, showCompleted, draggingGoalId, dragOverGoalId, goalInsertRef, goalDragFinalizedRef.",
    
    "C. Function cleanup:",
    "Remove goal helper functions ONLY if they are not used outside BudgetPanel:",
    "startEditGoal, saveEditGoal, addGoal, deleteGoal, toggleComplete, handleMarkDone, moveGoal, reorderGoalByDrag, finalizeGoalDrag, cleanupGoalDragState, onGoalDragStart, onGoalDragEnd.",
    
    "Before removal, verify each function is not referenced outside BudgetPanel.",
    
    "D. Memo/effect cleanup:",
    "Remove goal timeline logic including timelineBounds, timelineMonthSegments, rollingGoalTimeline, visibleTimelineSegments, goalTimelineScale, related useMemo blocks, auto-effects, lastGoalEW, and goal filtering logic.",
    
    "E. View/tab cleanup:",
    "Remove 'goals' from the BudgetPanel view tab array.",
    "Update view routing logic accordingly.",
    
    "F. JSX removal:",
    "Delete the entire JSX block tied to view === 'goals'.",
    
    "G. Import cleanup:",
    "Remove now-unused imports such as computeGoalTimeline, deriveRollingTimelineMonths, progressiveScale ONLY if not used elsewhere.",
    
    "H. Wiring audit (CRITICAL STEP):",
    "Before finalizing removals, trace where goals data originates and where it is consumed.",
    "Ensure removal does NOT break:",
    "- shared selectors",
    "- global state",
    "- any other panel relying on goals data",
    
    "If any dependency is shared, preserve it via read-only access rather than deleting it.",
    
    "I. Final pass:",
    "Remove dead code, unused variables, and orphaned references.",
    "Ensure BudgetPanel compiles cleanly and functions correctly without goals."
  ],

  "risk_areas": [
    "Accidentally removing shared goal logic still required elsewhere",
    "Breaking data flow for future GoalsPanel extraction",
    "Removing imports that are indirectly required",
    "Leaving stale references causing runtime errors",
    "Unintended dependency break between budget and goals math"
  ],

  "potential_bugs": [
    {
      "area": "dependency removal",
      "risk": "shared goal functions may be deleted prematurely"
    },
    {
      "area": "view routing",
      "risk": "removing goals tab may break tab index or state logic"
    },
    {
      "area": "imports",
      "risk": "removing imports still indirectly used may cause runtime crashes"
    },
    {
      "area": "selectors",
      "risk": "goal-related selectors may still be expected elsewhere"
    }
  ],

  "validation": [
    "BudgetPanel renders without goals UI",
    "No references to view === 'goals' remain",
    "No goal-specific state or handlers remain in BudgetPanel",
    "No unused imports remain",
    "BudgetPanel overview and breakdown still function correctly",
    "No console errors",
    "No TypeScript errors",
    "No unintended file changes"
  ]
}