{
  "task_id": "BUG_account_panel_signout_location_and_confirm_modal",
  "task_type": "bug",

  "environment_domain": "authority-finance-core",
  "allowed_environment_domains": [
    "authority-finance-core",
    "authority-finance-ui"
  ],
  "environment_reason": "Primary work is correcting the sign-out action path and placement so the proper account-level sign-out flow is used from the correct panel location. Small UI override is allowed only to move the entry point and add a confirmation modal.",
  "environment_policy": "scope_enforced_with_ui_override",
  "environment_runtime_visibility_policy": "report_if_available_do_not_fail_if_missing",

  "scope": {
    "allowed_files": [
      "account/profile panel components",
      "main account panel view/component",
      "sign-out action wiring/helpers",
      "small shared modal/dialog components or inline confirmation modal logic",
      "home panel only if needed to remove the wrongly added sign-out button",
      "directly affected tests only"
    ],
    "restricted_files": [
      "unrelated dashboard panels",
      "auth architecture refactors",
      "database/schema files",
      "tax/finance math logic",
      "navigation redesign",
      "broad account UI redesign"
    ]
  },

  "constraints": [
    "Remove the wrongly added Home panel sign-out entry if it exists",
    "Place the sign-out action on the main Account panel where intended",
    "Reuse the existing correct sign-out action path rather than creating a new logout implementation",
    "Add a confirmation modal before sign-out executes",
    "Keep UI changes small and localized",
    "Do not broaden into a full account-panel redesign"
  ],

  "implementation_steps": [
    "Inspect the current sign-out implementation and identify where the previous Codex change added the Home-panel sign-out button.",
    "Remove the Home-panel sign-out entry if it exists and is not desired.",
    "Inspect the main Account panel path and identify the correct place where the sign-out action should live.",
    "Add the sign-out action/button to the main Account panel where intended.",
    "Reuse the same underlying logout/sign-out action already used elsewhere in the app if available, rather than creating a parallel sign-out path.",
    "Add a confirmation modal/dialog that appears when the user clicks sign out.",
    "Ensure sign-out only executes after the user confirms in the modal.",
    "Ensure cancel closes the modal with no logout side effect.",
    "Keep the modal lightweight and consistent with existing dialog patterns if the app already has one.",
    "Verify the final sign-out flow works from the Account panel and no longer appears on the Home panel."
  ],

  "risk_areas": [
    "Home-panel sign-out may be removed visually but leave stale handlers or dead UI references behind",
    "Account-panel sign-out may render in the wrong subview instead of the intended main account surface",
    "Confirmation modal may appear but still allow logout to fire early if handler wiring is wrong",
    "A new modal path may duplicate instead of reuse the existing sign-out action"
  ],

  "potential_bugs": [
    {
      "area": "sign-out location",
      "risk": "sign-out may still appear in Home or may not appear in the intended Account panel location"
    },
    {
      "area": "confirmation flow",
      "risk": "logout may execute before confirmation or cancel may not properly abort"
    },
    {
      "area": "handler reuse",
      "risk": "a second sign-out implementation may diverge from the real auth logout path"
    },
    {
      "area": "UI cleanup",
      "risk": "removing the Home sign-out button may leave dead spacing or stale conditionals"
    }
  ],

  "validation": [
    "Home panel no longer shows the unwanted sign-out button",
    "Main Account panel shows the intended sign-out entry",
    "Clicking sign out opens a confirmation modal",
    "Cancel closes the modal and does not sign the user out",
    "Confirm signs the user out using the correct existing logout path",
    "No console errors",
    "No unintended file changes"
  ]
}