{
  "task_id": "AUDIT_FIX_nondhl_wizard_step2_path_recovery_pass_1",
  "task_type": "bug",

  "environment_domain": "authority-finance-audit",
  "allowed_environment_domains": [
    "authority-finance-audit",
    "authority-finance-core",
    "authority-finance-ui"
  ],
  "environment_reason": "This is an audit-first recovery task for the non-DHL setup wizard path. It requires reading the non-DHL wizard audit doc, tracing Step 2 setup state, and applying minimal fixes across wizard UI, config defaults, and setup validation so the standard employer path works again.",
  "environment_policy": "scope_enforced_with_core_and_ui_override",
  "environment_runtime_visibility_policy": "report_if_available_do_not_fail_if_missing",

  "scope": {
    "allowed_files": [
      "docs/non-DHL wizard audit file",
      "setup wizard components",
      "Step 2 wizard component",
      "wizard step definitions / validation",
      "DEFAULT_CONFIG / config defaults",
      "DHL preset toggle/reset logic",
      "non-DHL schedule/pay-scale setup logic",
      "minimal tests directly related to setup wizard Step 2"
    ],
    "restricted_files": [
      "dashboard panels unrelated to setup",
      "goal timeline UI",
      "budget UI",
      "tax projection logic unrelated to setup defaults",
      "database/schema migrations unless the audit proves persistence cannot work without one",
      "broad app refactors"
    ]
  },

  "constraints": [
    "Read the non-DHL wizard audit file before coding",
    "Focus specifically on Step 2 for this first recovery pass",
    "Pick up from the prior Claude work shown in the screenshot instead of restarting blindly",
    "Audit first, then apply minimal fixes",
    "Do not optimize the entire non-DHL flow in this task",
    "Do not break DHL preset behavior",
    "Non-DHL users must not inherit DHL-only assumptions",
    "DHL reset/toggle logic must restore normal defaults when DHL is turned off"
  ],

  "implementation_steps": [
    "Locate and read the non-DHL wizard audit file in the docs directory.",
    "Find the section related to Step 2 and read it fully.",
    "Inspect the current prior work referenced by the screenshot: Step 2 fixes, DEFAULT_CONFIG.scheduleIsVariable behavior, setDHL(true), and setDHL(false) reset behavior.",
    "Audit Step 2 state initialization for both DHL and non-DHL users.",
    "Trace all DHL-only setup assumptions that may leak into the non-DHL path, including schedule variables, pay scale options, deductions, overtime, differential fields, and preset resets.",
    "Confirm that DEFAULT_CONFIG.scheduleIsVariable is false by default and that setDHL(false) restores non-DHL-safe defaults instead of leaving DHL-specific values behind.",
    "Verify Step 2 validation does not require DHL-only fields for non-DHL users.",
    "Verify Step 2 next button behavior works for non-DHL users after valid inputs.",
    "Fix only the minimum broken pieces needed to make Step 2 functional for the non-DHL employer path.",
    "Add or preserve readback/debug comments only where helpful for future non-DHL recovery tasks.",
    "Do not continue into later wizard steps unless Step 2 cannot be validated without a tiny adjacent config fix."
  ],

  "risk_areas": [
    "setDHL(false) may leave DHL-specific config values active",
    "Step 2 validation may still require fields hidden from non-DHL users",
    "scheduleIsVariable may become null/undefined instead of false",
    "DHL preset fixes may regress while repairing non-DHL",
    "pay-scale or deduction defaults may leak across employer modes"
  ],

  "potential_bugs": [
    {
      "area": "DHL toggle reset",
      "risk": "switching from DHL to non-DHL may leave preset values such as pay period, OT, schedule, or deductions in config"
    },
    {
      "area": "Step 2 validation",
      "risk": "Next button may remain disabled because hidden DHL-only fields are still required"
    },
    {
      "area": "non-DHL config defaults",
      "risk": "non-DHL setup may save incomplete or undefined schedule/pay-scale values"
    },
    {
      "area": "DHL regression",
      "risk": "fixing non-DHL may accidentally remove expected DHL auto-assumptions"
    }
  ],

  "validation": [
    "Non-DHL wizard audit file was read",
    "Step 2 audit section was read",
    "Step 2 non-DHL path can proceed with valid non-DHL inputs",
    "setDHL(false) restores non-DHL-safe defaults",
    "scheduleIsVariable defaults to false when appropriate",
    "Hidden DHL-only fields do not block non-DHL Step 2 validation",
    "DHL preset path still behaves correctly",
    "No console errors",
    "No unintended file changes"
  ]
}