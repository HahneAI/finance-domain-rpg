{
  "task_id": "FEATURE_EXCLUDE_LOANS_FROM_MONTHLY_EXPENSE_CHANGE_SIGNAL",
  "task_type": "feature",
  "environment_domain": "authority-finance-ui",
  "allowed_environment_domains": ["authority-finance-ui"],
  "environment_reason": "This task changes UI signal behavior for monthly expense-change indicators, not core loan or expense math.",
  "environment_policy": "scope_enforced",
  "environment_runtime_visibility_policy": "report_if_available_do_not_fail_if_missing",
  "scope": {
    "objective": "Exclude loan changes from the monthly red-dot expense change signal so the indicator only reflects non-loan expense changes.",
    "entry_points": [
      "Budget monthly change signal UI",
      "Expense month indicator logic",
      "Expense list/category change detection"
    ],
    "allowed_files": [
      "src/components/BudgetPanel.jsx",
      "src/components/**/*.jsx",
      "src/lib/**/*.js",
      "src/**/*.css"
    ],
    "restricted_files": [
      "Do not modify loan payoff math.",
      "Do not modify expense calculation totals.",
      "Do not modify goal timeline math.",
      "Do not alter database schema unless already required by existing signal data."
    ]
  },
  "constraints": [
    "Loans must still count everywhere they currently count financially.",
    "Only the red-dot monthly expense-change signal should ignore loan changes.",
    "Non-loan expense changes should continue triggering the red-dot signal.",
    "Do not remove or hide loan cards/items from the UI.",
    "Keep edits minimal and localized to signal-detection logic."
  ],
  "implementation_steps": [
    "Find the logic that determines whether a month contains an expense change and renders the red-dot signal.",
    "Identify how loan expenses are represented in that data source.",
    "Update the signal detection so loan-category items are excluded before computing month-change status.",
    "Preserve existing behavior for all non-loan expenses.",
    "Confirm loan changes still affect monthly totals, payoff projections, and budget math outside this UI signal.",
    "Add a small helper if needed to clearly separate signal-eligible expenses from all financial expenses."
  ],
  "risk_areas": [
    "expense filtering → loan exclusion may accidentally remove loans from real budget totals",
    "category detection → loan items may use inconsistent category/type labels",
    "monthly indicators → red dot may disappear for valid non-loan changes if filtering is too broad",
    "shared helpers → changing reusable expense logic may affect unrelated panels"
  ],
  "potential_bugs": [
    "Loan changes stop affecting monthly cashflow totals",
    "Loan payoff/drop-off behavior changes unintentionally",
    "Red dot no longer appears for changed rent/food/needs expenses",
    "Only some loan types are excluded due to inconsistent labels",
    "Signal logic mutates the original expense array"
  ],
  "validation": [
    "Create a month where only a loan changes or drops off; confirm no red-dot expense change signal appears.",
    "Create a month where a non-loan expense changes; confirm the red-dot signal appears.",
    "Create a month where both loan and non-loan expenses change; confirm the red-dot signal appears because of the non-loan change.",
    "Confirm loan totals and payoff math remain unchanged.",
    "Confirm no unrelated UI layout changes.",
    "Run available lint/typecheck/test commands."
  ]
}