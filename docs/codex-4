{
  "task_id": "FEATURE_horizontal_stack_sprint2_audit_and_completion",
  "task_type": "feature",

  "environment_domain": "authority-finance-core",
  "allowed_environment_domains": [
    "authority-finance-core",
    "authority-finance-ui"
  ],
  "environment_reason": "Primary task is logic verification and feature completion of Sprint 2 in the horizontal stacks feature file. UI work is allowed only where required to complete the feature behavior.",
  "environment_policy": "scope_enforced_with_ui_override",
  "environment_runtime_visibility_policy": "report_if_available_do_not_fail_if_missing",

  "scope": {
    "allowed_files": [
      "horizontal stacks markdown file (Sprint source of truth)",
      "components implementing horizontal swipe stacks",
      "related hooks/state managing stack index or swipe behavior",
      "light UI adjustments strictly required to complete Sprint 2 behavior",
      "any helper logic directly referenced by Sprint 2"
    ],
    "restricted_files": [
      "unrelated UI components",
      "navigation system (unless directly required)",
      "core financial math (computeNet, buildYear, taxes, etc.)",
      "database/schema files",
      "unrelated feature modules"
    ]
  },

  "constraints": [
    "Treat the horizontal stacks markdown file as the single source of truth",
    "Do not re-interpret Sprint 2 — implement exactly what is defined",
    "First audit what is already implemented before writing new code",
    "Do not duplicate partially implemented logic — extend or complete it",
    "Only touch UI where required to complete Sprint 2 behavior",
    "Preserve existing Sprint 1 behavior completely",
    "Do not introduce new patterns if an existing pattern is already in use",
    "Keep all changes localized to the horizontal stack feature"
  ],

  "implementation_steps": [
    "Locate the horizontal stacks markdown file that defines the 5 sprints.",
    "Read Sprint 2 definition completely and treat it as the spec.",
    
    "AUDIT PHASE:",
    "Search the codebase for any existing implementation tied to Sprint 2.",
    "Identify:",
    "- completed parts",
    "- partially implemented parts",
    "- missing parts",
    "Map existing code to Sprint 2 requirements.",
    "Produce a clear internal mapping of 'implemented vs missing'.",

    "VALIDATION PHASE:",
    "Confirm whether Sprint 2 is:",
    "- not started",
    "- partially complete",
    "- incorrectly implemented",

    "IMPLEMENTATION PHASE:",
    "Complete missing Sprint 2 functionality using existing patterns.",
    "Fix incorrect implementations if they conflict with the markdown spec.",
    "Connect any missing state, handlers, or transitions required.",
    
    "UI OVERRIDE (ONLY IF REQUIRED):",
    "If Sprint 2 requires visible behavior (swipe, animation, stack transition, etc.),",
    "implement minimal UI logic necessary to make the feature function correctly.",
    "Do not redesign UI — only complete behavior.",
    
    "INTEGRATION:",
    "Ensure Sprint 2 integrates cleanly with:",
    "- Sprint 1 (already completed)",
    "- future stack progression model (Sprints 3–5 not broken)",
    
    "CLEANUP:",
    "Remove dead or duplicate partial implementations related to Sprint 2.",
    "Ensure no conflicting logic paths remain."
  ],

  "risk_areas": [
    "Partially implemented logic may conflict with final Sprint 2 requirements",
    "Swipe/stack state may become inconsistent if multiple sources of truth exist",
    "UI behavior may be decoupled from core state logic",
    "Existing Sprint 1 behavior could be accidentally affected",
    "Duplicate handlers or state variables may exist from earlier attempts"
  ],

  "potential_bugs": [
    {
      "area": "stack state",
      "risk": "index or active stack may desync from swipe gestures"
    },
    {
      "area": "partial implementation",
      "risk": "duplicate or conflicting logic paths may cause unpredictable behavior"
    },
    {
      "area": "UI override",
      "risk": "minimal UI changes may still introduce visual inconsistency if not aligned with existing patterns"
    },
    {
      "area": "integration",
      "risk": "Sprint 2 completion may break progression into Sprint 3 expectations"
    }
  ],

  "validation": [
    "Sprint 2 requirements fully match the markdown definition",
    "No duplicate or conflicting implementations remain",
    "Sprint 1 behavior remains unchanged",
    "Horizontal stack/swipe behavior functions correctly",
    "State transitions are consistent and predictable",
    "No console errors",
    "Changes are localized to the feature",
    "Future sprint structure remains intact"
  ]
}