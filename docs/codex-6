{
  "task_id": "FEATURE_swipeable_stacks_sprint_5_2_reorder_button_and_modal_shell",
  "task_type": "feature",

  "environment_domain": "authority-finance-core",
  "allowed_environment_domains": [
    "authority-finance-core",
    "authority-finance-ui"
  ],
  "environment_reason": "Primary responsibility is reading the sprint source-of-truth file, respecting sprint dependency order, and executing Sprint 5.2 exactly as specified. The implementation itself is UI-local, so limited UI override work is explicitly allowed.",
  "environment_policy": "scope_enforced_with_ui_override",
  "environment_runtime_visibility_policy": "report_if_available_do_not_fail_if_missing",

  "scope": {
    "allowed_files": [
      "docs/swipeable-stacks-sprint.md",
      "src/components/HomePanel.jsx",
      "small directly related styling or local helper references only if strictly required for Sprint 5.2"
    ],
    "restricted_files": [
      "state management outside HomePanel",
      "drag-and-drop reorder interaction logic for the modal",
      "touch reorder interaction logic",
      "desktop drag reorder logic",
      "database/schema files",
      "unrelated components",
      "future Sprint 5.3/5.4/5.5 implementation work"
    ]
  },

  "constraints": [
    "Treat the markdown sprint file as the source of truth",
    "Read Sprint 5 summary and Sprint 5.2 before making changes",
    "Implement Sprint 5.2 only",
    "Do not begin Sprint 5.3, 5.4, or 5.5",
    "Respect the independence of execution sprints",
    "Scope should remain limited to src/components/HomePanel.jsx unless the markdown explicitly requires otherwise",
    "State changes are allowed only if Sprint 5.2 explicitly calls for local modal shell state",
    "Do not implement reorder interaction yet",
    "Do not implement drag behavior in the modal shell yet",
    "Loose UI override is allowed only to complete the visual/modal-shell behavior described in Sprint 5.2"
  ],

  "implementation_steps": [
    "Open docs/swipeable-stacks-sprint.md and read the Sprint 5 summary section first.",
    "Read Sprint 5.2 in full and treat it as the exact implementation spec.",
    "Confirm the dependency framing: Sprint 5 is split into independently committable sub-sprints, and this task should implement 5.2 only.",
    "Inspect src/components/HomePanel.jsx only, unless Sprint 5.2 explicitly forces a tiny adjacent UI reference.",
    "Add only the new local state explicitly required by Sprint 5.2 for showing/hiding the reorder modal shell.",
    "Apply Sprint 5.2 card changes to both mobile and desktop card branches exactly as described in the markdown.",
    "Remove the existing up/down small-button pair where Sprint 5.2 says to remove it.",
    "Remove draggable/onDragStart/onDragEnd/onDragOver props from the desktop card branch where Sprint 5.2 says drag moves to the later modal-based sprint.",
    "Add the single REORDER button in the exact placement described by Sprint 5.2, including any conditional rendering requirements such as only when more than one active goal exists.",
    "Build the modal shell only, exactly as Sprint 5.2 describes, without implementing actual reorder interaction logic.",
    "Keep the work limited to shell/structure/presentational readiness for later Sprint 5.3 and 5.4 work.",
    "Do not implement touch reorder, desktop drag reorder inside modal, or QA pass items from later Sprint 5.x steps.",
    "Clean up only the small local code needed to make Sprint 5.2 render correctly and consistently."
  ],

  "risk_areas": [
    "Accidentally drifting into Sprint 5.3 or 5.4 interaction work",
    "Removing existing reorder affordances before the modal shell is wired cleanly",
    "Applying the change to only one card branch when the spec requires both mobile and desktop branches",
    "Over-editing HomePanel beyond the isolated Sprint 5.2 requirement"
  ],

  "potential_bugs": [
    {
      "area": "scope discipline",
      "risk": "implementation may bleed into later Sprint 5 interaction steps instead of stopping at shell-only"
    },
    {
      "area": "card branches",
      "risk": "REORDER button or removed controls may only be correct in one rendering branch"
    },
    {
      "area": "modal shell",
      "risk": "modal shell may mount incorrectly, overlap badly, or not close/open predictably if local state is wired inconsistently"
    },
    {
      "area": "existing controls",
      "risk": "removing drag/up-down controls may break expected temporary card actions before the modal shell is usable"
    }
  ],

  "validation": [
    "Sprint 5 summary was read before implementation",
    "Sprint 5.2 was read and treated as the source of truth",
    "Only Sprint 5.2 is implemented",
    "Only the local modal shell state required by Sprint 5.2 was added",
    "No reorder interaction logic was introduced yet",
    "Both card branches were updated if required by the spec",
    "REORDER button appears in the intended location and conditions",
    "Modal shell exists without interaction mode implementation",
    "No console errors",
    "No unintended file changes"
  ]
}