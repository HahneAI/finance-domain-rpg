{
  "task_id": "FEATURE_swipeable_stacks_sprint_5_3_touch_reorder_modal_interaction",
  "task_type": "feature",

  "environment_domain": "authority-finance-core",
  "allowed_environment_domains": [
    "authority-finance-core",
    "authority-finance-ui"
  ],
  "environment_reason": "Primary responsibility is reading the sprint source-of-truth file, respecting Sprint 5 dependency order, and implementing Sprint 5.3 exactly as specified. The implementation is UI-local interaction work, so limited UI override is explicitly allowed.",
  "environment_policy": "scope_enforced_with_ui_override",
  "environment_runtime_visibility_policy": "report_if_available_do_not_fail_if_missing",

  "scope": {
    "allowed_files": [
      "docs/swipeable-stacks-sprint.md",
      "src/components/HomePanel.jsx",
      "small directly related local styling or helper references only if strictly required for Sprint 5.3"
    ],
    "restricted_files": [
      "state management outside HomePanel",
      "desktop drag reorder interaction from Sprint 5.4",
      "QA pass work from Sprint 5.5",
      "database/schema files",
      "unrelated components",
      "future Sprint 5.4/5.5 implementation work"
    ]
  },

  "constraints": [
    "Treat the markdown sprint file as the source of truth",
    "Read Sprint 5 summary and Sprint 5.3 before making changes",
    "Implement Sprint 5.3 only",
    "Do not begin Sprint 5.4 or 5.5",
    "Respect the independence of execution sprints",
    "Scope should remain limited to src/components/HomePanel.jsx unless the markdown explicitly requires otherwise",
    "State changes are allowed only where Sprint 5.3 explicitly requires local modal interaction state",
    "Do not implement desktop drag reorder behavior yet",
    "Preserve the Sprint 5.2 modal shell and extend it rather than rebuilding it",
    "Loose UI override is allowed only to complete the touch/tap-select arrow interaction described in Sprint 5.3"
  ],

  "implementation_steps": [
    "Open docs/swipeable-stacks-sprint.md and read the Sprint 5 summary section first.",
    "Read Sprint 5.3 in full and treat it as the exact implementation spec.",
    "Confirm the dependency framing: Sprint 5 is split into independently committable sub-sprints, and this task should implement 5.3 only.",
    "Inspect src/components/HomePanel.jsx only, unless Sprint 5.3 explicitly forces a tiny adjacent UI reference.",
    "Build on top of the existing Sprint 5.2 modal shell rather than replacing it.",
    "Add only the new local state explicitly required by Sprint 5.3 for the selected mini-card in the reorder modal.",
    "Implement tap-select behavior on the mini-cards so the selected card is tracked by id and visually distinguished exactly as the markdown describes.",
    "Reset the selected-card state to null when the modal closes if the sprint spec requires that.",
    "Render the left/right arrow buttons below the ScrollSnapRow exactly as specified, including any visibility rules such as only when coarse pointer is true.",
    "Implement the bounds-safe wrapper around moveGoal exactly as described by the sprint spec so movement stays within the active-goals list only.",
    "Use active-only indexing guards so the selected card cannot move into the completed-goals section.",
    "Wire canMoveLeft and canMoveRight exactly as required by the active-goals index rules.",
    "Do not implement desktop drag reorder behavior from Sprint 5.4.",
    "Keep all work limited to touch reorder interaction inside the modal introduced in Sprint 5.2.",
    "Clean up only the small local code needed to make Sprint 5.3 work correctly and consistently."
  ],

  "risk_areas": [
    "Accidentally drifting into Sprint 5.4 desktop drag behavior",
    "Using raw goals-array movement without active-only bounds protection",
    "Selected-card state may persist incorrectly after modal close",
    "Arrow interaction may work in one pointer mode but not honor the coarse-pointer visibility rule",
    "Over-editing HomePanel beyond the isolated Sprint 5.3 requirement"
  ],

  "potential_bugs": [
    {
      "area": "active-only bounds",
      "risk": "selected active goal may move across the active/completed boundary if the wrapper is not used correctly"
    },
    {
      "area": "selection state",
      "risk": "reorderSelectedId may remain stale after modal close or after a move"
    },
    {
      "area": "arrow controls",
      "risk": "left/right buttons may render or enable incorrectly if canMoveLeft/canMoveRight logic is off"
    },
    {
      "area": "scope discipline",
      "risk": "implementation may bleed into Sprint 5.4 desktop drag logic instead of stopping at Sprint 5.3"
    }
  ],

  "validation": [
    "Sprint 5 summary was read before implementation",
    "Sprint 5.3 was read and treated as the source of truth",
    "Only Sprint 5.3 is implemented",
    "Only the local state required by Sprint 5.3 was added",
    "Tap-select works on mini-cards in the reorder modal",
    "Selected card styling appears as specified",
    "Arrow buttons move goals only within the active-goals list",
    "Selection resets on modal close if required by the sprint",
    "No desktop drag reorder behavior was introduced yet",
    "No console errors",
    "No unintended file changes"
  ]
}