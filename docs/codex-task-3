# Codex Task Specification

# =========================
# TASK_SPEC (MACHINE READABLE)
# =========================

```json
{
  "task_id": "FEATURE_goals_timeline_monthly_weekly_grid_rebuild",
  "task_type": "FEATURE",
  "status": "PENDING",

  "objective": "Rebuild the goals timeline tracker so it renders as a month-labeled horizontal timeline with subtle internal week subdivisions. Each month section should visually contain 4 smaller bar blocks representing week-level progress units, and goal bars should extend proportionally into a month based on target completion timing, including partial progress into the month such as mid-month deadlines. The implementation must avoid breaking the goals tab again.",

  "scope": {
    "allowed_files": [
      "goals tab/page component",
      "goals timeline component",
      "goal bar rendering component",
      "goal date-to-position calculation helpers",
      "timeline/grid styling related specifically to the goals tracker",
      "goal model/view-model logic needed to map dates into timeline positions"
    ],
    "restricted_files": [
      "unrelated dashboard tabs/pages",
      "expense/budget components",
      "authentication logic",
      "global layout system",
      "database schema unless already required by current goal date fields",
      "unrelated shared chart components unless already used directly by the goals timeline"
    ],
    "entry_points": [
      "goals tab main render flow",
      "goals timeline/grid rendering",
      "goal item bar positioning logic",
      "date normalization helpers used by the goals tab"
    ],
    "out_of_scope": [
      "goal creation form redesign",
      "new goal categories or statuses",
      "backend changes unrelated to existing goal date fields",
      "major redesign of the entire goals tab outside the tracker",
      "daily-resolution timeline rendering beyond the monthly + 4-block weekly subdivision model"
    ]
  },

  "implementation": {
    "steps": [
      "Inspect the current goals tab and identify the prior timeline implementation points that caused the tab to break.",
      "Rebuild or replace the timeline renderer in a minimal, isolated way so the goals tab remains stable even if the new visualization logic fails gracefully.",
      "Render the timeline as month-labeled sections across the target year or active time range used by the current goals view.",
      "Within each month section, render 4 subtle internal subdivision blocks to visually represent week-level chunks.",
      "Map each goal's target date range or completion date into this month-plus-week grid so the visual bar extends proportionally into the correct month section.",
      "Ensure a goal due around the middle of a month visually ends around the halfway point of that month's section rather than snapping only to month boundaries.",
      "Keep the weekly subdivisions slightly visible and supportive, without overpowering the primary month labels.",
      "Preserve or restore goals tab functionality, loading state, and rendering behavior so the tab does not break from timeline changes."
    ],
    "constraints": [
      "Do not modify unrelated tabs or app-wide layout",
      "Preserve existing TypeScript types",
      "Prefer a contained rebuild of the goals timeline over risky broad refactors",
      "Fail safely if date values are incomplete or malformed",
      "Keep month labels primary and week subdivisions secondary/subtle",
      "Do not break existing goals data rendering outside the timeline visualization"
    ]
  },

  "change_log": {
    "expected_changes": [
      {
        "file": "goals timeline component",
        "change": "Render month-labeled sections with 4 subtle internal week blocks per month"
      },
      {
        "file": "goal bar rendering logic",
        "change": "Position and size goal bars proportionally into month sections based on date timing, including mid-month placement"
      },
      {
        "file": "date mapping helper(s)",
        "change": "Translate goal target dates into monthly section and intra-month weekly offsets"
      },
      {
        "file": "goals tab/page integration",
        "change": "Stabilize timeline mounting/render flow so the goals tab continues working after the visualization update"
      }
    ]
  },

  "impact_analysis": {
    "affected_areas": [
      "goals tab rendering",
      "timeline/grid visualization",
      "goal bar positioning",
      "date-to-UI mapping for goals"
    ],
    "risk_areas": [
      "goals tab may fail to render if timeline calculations throw",
      "month width and week subdivision math may misalign",
      "mid-month positioning may be visually off",
      "date normalization/timezone handling may shift goals into wrong sections",
      "responsive layout may compress labels or bars incorrectly"
    ],
    "potential_bugs": [
      {
        "area": "goals tab stability",
        "risk": "timeline rebuild may break the entire goals tab again if rendering logic is not isolated and guarded"
      },
      {
        "area": "date mapping",
        "risk": "goals may render in the wrong month or wrong partial-month position"
      },
      {
        "area": "week subdivision logic",
        "risk": "4 internal blocks may not line up consistently across all month widths"
      },
      {
        "area": "bar sizing",
        "risk": "goal bars may overshoot, undershoot, or snap incorrectly at month boundaries"
      },
      {
        "area": "responsive UI",
        "risk": "month labels, subtle week blocks, or bars may overlap or disappear on smaller screens"
      }
    ]
  },

  "validation": {
    "checks": [
      "Goals tab renders without breaking",
      "Timeline shows month labels clearly",
      "Each month contains 4 subtle internal subdivision blocks",
      "Goal bars extend proportionally into month sections rather than snapping only to month boundaries",
      "A mid-month goal visually ends around the halfway point of the month section",
      "Timeline remains readable on the current supported screen sizes",
      "No console errors",
      "No TypeScript errors",
      "No unintended file changes"
    ]
  },

  "output_format": {
    "require_summary": true,
    "summary_fields": [
      "files_changed",
      "changes_made",
      "timeline_mapping_rules",
      "stability_guards_added",
      "new_risks_detected"
    ]
  }
}