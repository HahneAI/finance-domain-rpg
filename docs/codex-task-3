{
  "task_id": "AUDIT_state_tax_systems_all_50_states_and_fix_missouri_accuracy",
  "task_type": "FEATURE",
  "status": "PENDING",

  "objective": "Audit and correct the internal 50-state tax table to ensure each state is correctly classified (no tax, flat, or bracketed), and verify that Missouri's progressive tax calculation is implemented correctly using marginal bracket logic.",

  "scope": {
    "allowed_files": [
      "state tax table/config",
      "tax calculation helpers",
      "income tax computation pipeline",
      "state selection logic",
      "any helper functions applying tax rates"
    ],
    "restricted_files": [
      "UI components unrelated to tax display",
      "auth logic",
      "goals/budget unrelated logic",
      "federal tax logic unless directly tied"
    ],
    "entry_points": [
      "state tax lookup table",
      "tax calculation function",
      "income → tax pipeline"
    ]
  },

  "implementation": {
    "steps": [
      "Audit the existing state tax table and classify each state as no-tax, flat, or bracketed.",
      "Verify flat states apply a single consistent rate across all income.",
      "Verify bracketed states use marginal tax logic instead of flat application.",
      "Focus deeply on Missouri implementation and confirm correct bracket thresholds and marginal calculation.",
      "Fix any states incorrectly treated as flat or non-taxed.",
      "Ensure tax calculation function dynamically handles all three system types.",
      "Document assumptions and ensure future expandability."
    ],
    "constraints": [
      "Do not hardcode incorrect flat approximations for bracketed states",
      "Preserve existing TypeScript types",
      "Keep system modular for future updates",
      "Prefer data-driven tax tables over logic branching"
    ]
  },

  "validation": {
    "checks": [
      "All 50 states correctly classified",
      "Missouri tax calculation matches marginal bracket logic",
      "Flat states apply one rate correctly",
      "No-tax states return zero tax",
      "No console errors",
      "No incorrect tax inflation across income levels"
    ]
  },

  "output_format": {
    "summary_fields": [
      "states_classification_fixed",
      "missouri_logic_verified",
      "tax_calculation_method",
      "edge_cases_found"
    ]
  }
}