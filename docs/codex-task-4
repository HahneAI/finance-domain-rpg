# Codex Task Specification

# =========================
# TASK_SPEC (MACHINE READABLE)
# =========================

```json
{
  "task_id": "AUDIT_dhl_benefits_deduction_pipeline_and_account_edit_flow",
  "task_type": "FEATURE",
  "status": "PENDING",

  "objective": "Audit and complete the DHL benefits deduction pipeline so all DHL payroll-benefit options are fully wired as real pre-net paycheck deductions, editable from the Account/Profile tab, and immediately reflected after save in weekly take-home/paycheck calculations. Do not add default values or force new inputs yet—only ensure the full pipeline is correctly connected and streamlined.",

  "scope": {
    "allowed_files": [
      "account/profile tab benefits settings components",
      "benefits tab/page components",
      "benefits data model/types",
      "DHL preset benefits configuration",
      "paycheck deduction calculation helpers",
      "take-home / net-pay computation pipeline",
      "state selectors/hooks feeding benefits into pay calculations",
      "save/update handlers for employee benefit selections and weekly deduction amounts",
      "benefits summary display components where deductions are shown"
    ],
    "restricted_files": [
      "config tab components",
      "unrelated dashboard tabs/pages",
      "authentication logic",
      "tax-bracket/state-tax logic except where net-pay order depends on pre-net deduction flow",
      "goal timeline visuals",
      "expense logic unrelated to paycheck deductions",
      "global theme system"
    ],
    "entry_points": [
      "account/profile tab benefits edit flow",
      "DHL preset benefits config source",
      "benefits-to-paycheck deduction mapping",
      "net take-home calculation pipeline",
      "save action for benefits updates"
    ],
    "out_of_scope": [
      "adding new default values",
      "finalizing real carrier/plan defaults",
      "moving benefits editing into new tabs",
      "config-tab benefit editing",
      "broad profile tab redesign",
      "new benefit categories outside the existing DHL preset set"
    ]
  },

  "implementation": {
    "steps": [
      "Inspect the current DHL preset and identify every configured payroll-deducted benefit option, including but not limited to 401k, bucket hours if relevant to deductions, short-term disability, long-term disability, dental, vision, and medical.",
      "Build an explicit audit list of all current DHL benefit deduction options in the app and confirm whether the preset has 9 or 10 payroll-deduction benefit entries.",
      "Audit whether each benefit option is exposed for editing in the Account/Profile tab and confirm the update path is live.",
      "Ensure benefits are editable from the Account/Profile tab only for now, and remove or disable duplicate editing exposure elsewhere if currently present outside intended surfaces, especially config.",
      "Trace the full pipeline from benefit edit/save -> stored state -> selector/hook -> paycheck math -> displayed take-home values.",
      "Verify each benefit is treated as a real paycheck deduction that reduces take-home in the intended pre-net deduction stage of the app's current pay model.",
      "If the app distinguishes between pre-tax and post-tax deductions internally, ensure each DHL benefit is mapped to the correct deduction stage according to the current architecture; if the architecture only supports one pre-net deduction stage right now, ensure all intended benefit deductions consistently flow through that stage.",
      "Ensure saving updated benefit selections or weekly deduction amounts immediately triggers recalculation of weekly deduction totals and take-home/paycheck outputs.",
      "Audit all benefits-related tabs/cards/summary surfaces so they read from the same source of truth and do not show stale or disconnected values.",
      "Do not invent missing default numbers; only ensure the wiring, save behavior, and deduction pipeline are complete and ready for real values."
    ],
    "constraints": [
      "Do not add arbitrary default values",
      "Preserve existing TypeScript types",
      "Keep the benefits edit surface centered in Account/Profile for now",
      "Prefer one benefits source of truth across profile, benefits summaries, and paycheck math",
      "Keep changes focused on deduction wiring and editability, not UI redesign",
      "Do not touch unrelated budget systems beyond the deduction pipeline integration"
    ]
  },

  "change_log": {
    "expected_changes": [
      {
        "file": "DHL preset benefits config",
        "change": "Audit and normalize the full set of DHL payroll-deduction benefit options"
      },
      {
        "file": "account/profile benefits settings UI",
        "change": "Ensure all DHL benefit options are editable and saved from the intended profile/account surface"
      },
      {
        "file": "benefits model/selectors/hooks",
        "change": "Unify source-of-truth flow from saved benefits into paycheck deduction logic"
      },
      {
        "file": "net pay / take-home calculation helpers",
        "change": "Ensure all configured DHL benefits immediately reduce weekly take-home according to the intended deduction stage"
      },
      {
        "file": "duplicate exposure paths outside account/profile",
        "change": "Remove, disable, or stop relying on unintended editing surfaces if they create conflicts"
      }
    ]
  },

  "impact_analysis": {
    "affected_areas": [
      "account/profile tab benefits editing",
      "DHL preset benefits configuration",
      "weekly paycheck deduction math",
      "take-home calculations",
      "benefits summary/read-only surfaces"
    ],
    "risk_areas": [
      "some benefits may be present in config/data but not actually connected to paycheck math",
      "save actions may update UI state without recalculating take-home totals",
      "different tabs may read different benefit sources and show inconsistent values",
      "pre-net/pre-tax deduction ordering may be inconsistently applied",
      "duplicate edit surfaces may conflict with the account/profile source of truth"
    ],
    "potential_bugs": [
      {
        "area": "benefit wiring",
        "risk": "one or more DHL benefit options may exist visually but not feed into paycheck deduction math"
      },
      {
        "area": "save pipeline",
        "risk": "editing a weekly deduction may save but not immediately update take-home outputs"
      },
      {
        "area": "source-of-truth consistency",
        "risk": "account/profile, benefits tab, and budget calculations may each read different benefit state"
      },
      {
        "area": "deduction stage",
        "risk": "benefits may be subtracted at the wrong stage, causing incorrect take-home numbers"
      },
      {
        "area": "UI exposure",
        "risk": "benefits may still be editable from unintended locations such as config, creating conflicting updates"
      }
    ]
  },

  "validation": {
    "checks": [
      "All DHL payroll-deduction benefit options are identified and audited",
      "All intended DHL benefit options are editable from the Account/Profile tab",
      "Unintended duplicate edit exposure outside Account/Profile is removed or no longer authoritative",
      "Saving a benefit update immediately recalculates weekly deductions and take-home values",
      "All benefit deductions flow into paycheck math as real pre-net deductions according to the current architecture",
      "Benefits-related surfaces read from the same source of truth",
      "No console errors",
      "No TypeScript errors",
      "No unintended file changes"
    ]
  },

  "output_format": {
    "require_summary": true,
    "summary_fields": [
      "files_changed",
      "dhl_benefit_options_audited",
      "benefits_wired_and_editable",
      "deduction_pipeline_verified",
      "duplicate_edit_surfaces_found",
      "remaining_edge_cases"
    ]
  }
}