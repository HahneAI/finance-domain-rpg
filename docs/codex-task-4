# Codex Task Specification

# =========================
# TASK_SPEC (MACHINE READABLE)
# =========================

```json
{
  "task_id": "FEATURE_rolling_active_view_hide_old_data_prepare_full_year_review",
  "task_type": "FEATURE",
  "status": "PENDING",

  "objective": "Convert the income summary weekly tab and the goals timeline tracker into rolling active-period views where older data progressively disappears from the main app view as the year advances, while preserving all historical data for a future full-year review tab. The active views should keep only recent relevant periods visible and slightly enlarge remaining visible elements in a subtle, consistent way.",

  "scope": {
    "allowed_files": [
      "income summary weekly tab component",
      "goals timeline tracker component",
      "shared timeline/date window helpers",
      "timeline sizing/layout logic for rolling active views",
      "supabase migrations folder if persistence is truly needed",
      "db types or data access files if schema changes are required"
    ],
    "restricted_files": [
      "authentication logic",
      "expense editor logic",
      "unrelated dashboard tabs/pages",
      "full-year review tab implementation beyond light preparation hooks",
      "global theme system",
      "unrelated database tables"
    ],
    "entry_points": [
      "income weekly tab render flow",
      "goals timeline render flow",
      "date window calculations",
      "migrations folder inspection",
      "timeline visibility filter logic"
    ],
    "out_of_scope": [
      "building the future full-year review tab now",
      "deleting historical data",
      "rewriting unrelated budgeting logic",
      "large app-wide layout refactors"
    ]
  },

  "implementation": {
    "steps": [
      "Inspect the current income weekly tab and goals timeline tracker rendering logic and identify where visible date ranges are calculated.",
      "Implement a rolling active-view filter so older periods disappear from the main app view as time advances, without deleting underlying historical data.",
      "For weekly rolling views, keep the last four completed weeks before the current app date visible, plus the current period only if the existing UX requires it.",
      "For the goals timeline tracker, hide older months/segments from the main active view as they age out, while preserving underlying goal timeline records.",
      "As older periods fall off, scale the remaining visible elements slightly larger in a subtle, consistent way without causing layout breakage.",
      "Structure the filtering logic so the hidden older data can later be reused by a separate full-year review tab.",
      "Inspect the migrations folder before making any schema change.",
      "Only create a Supabase migration if the current architecture truly needs persisted user-specific active-view settings or archived timeline metadata rather than deriving visibility from dates."
    ],
    "constraints": [
      "Do not delete historical data",
      "Do not build the full-year review tab in this task",
      "Preserve existing TypeScript types",
      "Prefer deriving active-view visibility from existing dates over adding new schema",
      "If schema changes are required, keep them minimal and user-scoped",
      "Keep visual enlargement subtle and stable"
    ]
  },

  "change_log": {
    "expected_changes": [
      {
        "file": "income summary weekly tab component",
        "change": "Hide older weekly data from main active view and keep only recent rolling periods visible"
      },
      {
        "file": "goals timeline tracker component",
        "change": "Hide older month/timeline segments from the main active view while preserving data for later yearly review"
      },
      {
        "file": "shared timeline/date helpers",
        "change": "Add reusable rolling active-view filter logic that can later support a full-year review tab"
      },
      {
        "file": "supabase migration file (optional)",
        "change": "Only add minimal user-scoped persistence if deriving visibility from current dates is insufficient"
      }
    ]
  },

  "impact_analysis": {
    "affected_areas": [
      "income weekly tab rendering",
      "goals timeline rendering",
      "date filtering/window logic",
      "future compatibility with yearly review surfaces"
    ],
    "risk_areas": [
      "wrong periods may disappear too early",
      "weekly boundary math may be off",
      "month hiding rules may remove useful recent goal context",
      "slight scaling may cause overlap or layout compression",
      "schema changes may be unnecessary if derivation is sufficient"
    ],
    "potential_bugs": [
      {
        "area": "rolling weekly filter",
        "risk": "the wrong four prior weeks may remain visible or disappear"
      },
      {
        "area": "goals monthly filter",
        "risk": "older goal segments may hide too aggressively or not enough"
      },
      {
        "area": "future yearly review compatibility",
        "risk": "main-view hiding logic may be implemented in a way that is hard to reuse later for a full-year tab"
      },
      {
        "area": "visual scaling",
        "risk": "remaining visible elements may misalign, clip, or shift unexpectedly"
      },
      {
        "area": "data preservation",
        "risk": "hidden data may appear lost if the UI does not clearly preserve source records"
      }
    ]
  },

  "validation": {
    "checks": [
      "Older data disappears from the main app view as time advances",
      "Historical data remains preserved and queryable",
      "Income weekly tab shows only the intended recent rolling window",
      "Goals tracker hides aged-out timeline sections from the active view",
      "Remaining visible elements scale slightly larger in a subtle, consistent way",
      "Implementation leaves the data structure usable for a future full-year review tab",
      "No console errors",
      "No TypeScript errors",
      "No unintended file changes"
    ]
  },

  "output_format": {
    "require_summary": true,
    "summary_fields": [
      "files_changed",
      "changes_made",
      "rolling_visibility_rules",
      "future_full_year_review_preparation",
      "migration_created",
      "new_risks_detected"
    ]
  }
}