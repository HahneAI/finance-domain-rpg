# Codex Task Specification

# =========================
# TASK_SPEC (MACHINE READABLE)
# =========================

```json
{
  "task_id": "FEATURE_income_weekly_overview_chart_sticky_header",
  "task_type": "FEATURE",
  "status": "PENDING",

  "objective": "Add a careful sticky-header behavior to the large chart/table in the Income tab weekly overview subview so the category/column header names remain visible at the top of the screen while scrolling through the chart. The header should appear to separate cleanly from the chart and stay pinned once the chart header reaches the top of the viewport, without drastic layout changes.",

  "scope": {
    "allowed_files": [
      "income tab weekly overview subview/component",
      "large chart/table component used in the weekly overview",
      "chart header / column label row component",
      "local scroll container logic for the chart if present",
      "minimal styling for sticky positioning and layering"
    ],
    "restricted_files": [
      "income config subview",
      "tax settings pages",
      "budget tab components",
      "goals tab components",
      "global layout system",
      "authentication logic",
      "chart data calculation helpers unless needed for header rendering only"
    ],
    "entry_points": [
      "income weekly overview render flow",
      "chart/table header row render path",
      "chart scroll container",
      "column label component"
    ],
    "out_of_scope": [
      "chart redesign",
      "changing chart data/math",
      "virtualization rewrite",
      "broad responsive layout refactor",
      "sticky behavior for unrelated tables/charts"
    ]
  },

  "implementation": {
    "steps": [
      "Inspect the Income tab weekly overview subview and identify the large chart/table structure, including the column/category header row and the scroll container behavior.",
      "Implement a sticky header for the chart so the header row pins to the top when the chart header reaches the top of the viewport or local scroll container, depending on the current architecture.",
      "Ensure the sticky header remains aligned with the chart columns while the user scrolls vertically through the chart.",
      "Make the pinned header visually feel like it separates from the chart cleanly, while keeping the design subtle and not drastic.",
      "Preserve normal scrolling behavior and avoid changing the chart’s core layout or data rendering.",
      "Handle z-index, background, and spacing carefully so underlying rows do not visually bleed through the sticky header.",
      "If horizontal scrolling exists, ensure the sticky header stays synchronized with the chart columns.",
      "Keep the implementation minimal and localized to this weekly overview chart."
    ],
    "constraints": [
      "Do not redesign the chart",
      "Preserve existing TypeScript types",
      "Use a minimal sticky-header solution if the current layout allows it",
      "Avoid broad CSS/layout changes outside the chart component",
      "Keep the effect subtle and stable",
      "Do not break column alignment or scrolling behavior"
    ]
  },

  "change_log": {
    "expected_changes": [
      {
        "file": "income weekly overview chart/table component",
        "change": "Add sticky header behavior for the category/column names"
      },
      {
        "file": "chart header row component or markup",
        "change": "Support pinned rendering/state while preserving column alignment"
      },
      {
        "file": "local chart styling",
        "change": "Add minimal sticky positioning, background, and layering styles needed for stable header pinning"
      }
    ]
  },

  "impact_analysis": {
    "affected_areas": [
      "income weekly overview chart scrolling UX",
      "chart header visibility",
      "column alignment during scroll"
    ],
    "risk_areas": [
      "sticky header may misalign with columns",
      "header may overlap content incorrectly",
      "z-index/background may cause rows to show through",
      "local versus page scroll container detection may be wrong",
      "horizontal scrolling may desync header and content if present"
    ],
    "potential_bugs": [
      {
        "area": "sticky positioning",
        "risk": "header may not pin at the right moment or may pin relative to the wrong scroll container"
      },
      {
        "area": "column alignment",
        "risk": "pinned headers may drift from underlying chart columns"
      },
      {
        "area": "visual layering",
        "risk": "chart rows may bleed through the sticky header or the header may cover too much content"
      },
      {
        "area": "responsive layout",
        "risk": "sticky header behavior may break on smaller screens or if column widths change"
      }
    ]
  },

  "validation": {
    "checks": [
      "Chart/category header names stay visible while scrolling through the weekly overview chart",
      "Header pins cleanly when it reaches the top of the viewport or intended scroll container",
      "Sticky header remains aligned with the chart columns",
      "Scrolling behavior remains smooth and intact",
      "No drastic visual changes were introduced",
      "No console errors",
      "No TypeScript errors",
      "No unintended file changes"
    ]
  },

  "output_format": {
    "require_summary": true,
    "summary_fields": [
      "files_changed",
      "sticky_header_implementation",
      "scroll_container_used",
      "alignment_handling",
      "remaining_edge_cases"
    ]
  }
}