{
  "task": "AUDIT_EXPENSE_CALCULATION_STEPS",
  "objective": "Trace each step of the expense calculation from income through quarterly splits and log every step in a Markdown file.",
  "implementation_steps": [
    "Start at income input calculation functions.",
    "Log each routing step where income transforms into intermediary values.",
    "Track any multipliers or adjustments applied.",
    "Identify where quarterly splits occur in the code.",
    "Compare weekly derivations versus quarterly targets.",
    "Log each transition point into a designated audit Markdown file."
  ],
  "constraints": "No modifying calculations, only trace and log steps.",
  "risk_areas": ["Missed step if code path skipped", "Potential performance overhead during audit"]
}