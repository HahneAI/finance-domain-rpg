# Codex Task Specification

# =========================
# TASK_SPEC (MACHINE READABLE)
# =========================

```json
{
  "task_id": "FEATURE_log_tab_hero_cleanup_and_effect_summary_card",
  "task_type": "FEATURE",
  "status": "PENDING",

  "objective": "Simplify the log tab layout by keeping only the net loss card, PTO loss card, and bucket hours loss card in the small square-card section; preserve the larger week naming card and bucket hour bar card; consolidate the remaining pre-event-log metrics/content into one large 'Log Effect Summary' component card with no visual background separation between its internal data points; and remove the explanatory text box at the bottom of the log tab.",

  "scope": {
    "allowed_files": [
      "log tab/page component",
      "log tab hero section component",
      "small metric card grid component(s) used in the log tab",
      "bucket hour bar card component if minor layout integration is needed",
      "pre-event-log summary content components",
      "new or updated Log Effect Summary card component",
      "bottom explanatory text box component/path in the log tab"
    ],
    "restricted_files": [
      "event log history list/item rendering",
      "authentication logic",
      "income tab components",
      "budget tab components",
      "goals timeline components",
      "global theme system beyond minor local styling needed for this cleanup",
      "week naming card logic",
      "bucket balance/bar logic except for preserving placement/layout compatibility"
    ],
    "entry_points": [
      "log tab render flow",
      "hero section card grid render path",
      "pre-event-log content render path above event history",
      "bottom explanatory text section render path"
    ],
    "out_of_scope": [
      "changing the event log history data structure or behavior",
      "rewriting log calculations",
      "altering the larger week naming card content",
      "altering the bucket hour bar behavior/content",
      "broad dashboard redesign outside the log tab"
    ]
  },

  "implementation": {
    "steps": [
      "Inspect the current log tab layout and identify the hero section small square metric cards, the larger week naming card, the larger bucket hour bar card, the pre-event-log content area, and the bottom explanatory text box.",
      "Using the current UI shown in the screenshot as reference, remove the unnecessary small square cards so only the net loss card, PTO loss card, and bucket hours loss card remain in that small-card section.",
      "Leave the larger week naming card and the larger bucket hour bar card intact.",
      "Take the remaining content above the event log history that is currently spread across separate UI pieces and consolidate it into one larger component card titled 'Log Effect Summary'.",
      "Inside the new Log Effect Summary card, present the compiled data chunks without separate internal background blocks/cards between each datapoint.",
      "Keep the layout visually cleaner and more consolidated while preserving the underlying data shown.",
      "Remove the explanatory text box at the bottom of the log tab.",
      "Ensure the event log history section remains intact and correctly positioned below the cleaned-up summary area."
    ],
    "constraints": [
      "Do not modify the event log history behavior",
      "Preserve existing TypeScript types",
      "Keep the week naming card and bucket hour bar card unchanged except for necessary spacing/layout adjustments",
      "Do not redesign unrelated tabs",
      "Prefer restructuring existing components over broad refactors",
      "Maintain the current visual language while simplifying component count"
    ]
  },

  "change_log": {
    "expected_changes": [
      {
        "file": "log tab hero component",
        "change": "Remove extra small square cards and keep only net loss, PTO loss, and bucket hours loss cards"
      },
      {
        "file": "log tab summary/pre-history component(s)",
        "change": "Consolidate remaining data above event history into one large 'Log Effect Summary' card"
      },
      {
        "file": "log tab page layout",
        "change": "Clean up spacing/order while preserving the larger week naming and bucket bar cards"
      },
      {
        "file": "bottom explanatory text box component/path",
        "change": "Remove the explanatory text box from the bottom of the log tab"
      }
    ]
  },

  "impact_analysis": {
    "affected_areas": [
      "log tab hero section",
      "log tab summary layout above event history",
      "bottom-of-tab explanatory content",
      "spacing and card hierarchy in the log tab"
    ],
    "risk_areas": [
      "removing cards may break grid spacing or alignment",
      "consolidating summary content may accidentally drop important datapoints",
      "new single summary card may become visually dense if spacing is not handled well",
      "layout cleanup may shift event log history position unexpectedly"
    ],
    "potential_bugs": [
      {
        "area": "hero section card grid",
        "risk": "removing extra cards may leave broken gaps, wrong column spans, or inconsistent card sizing"
      },
      {
        "area": "Log Effect Summary",
        "risk": "some pre-event-log datapoints may be omitted or duplicated during consolidation"
      },
      {
        "area": "layout flow",
        "risk": "event log history may be pushed too far down or overlap with summary content"
      },
      {
        "area": "component cleanup",
        "risk": "removing the bottom explanatory text box may leave unused spacing or dead code paths"
      }
    ]
  },

  "validation": {
    "checks": [
      "Only the net loss, PTO loss, and bucket hours loss small cards remain in the log tab hero section",
      "The larger week naming card remains intact",
      "The larger bucket hour bar card remains intact",
      "All remaining pre-event-log content is consolidated into a single 'Log Effect Summary' card",
      "The Log Effect Summary card has no internal card/background separation between datapoints",
      "The explanatory text box at the bottom of the log tab is removed",
      "The event log history remains intact and correctly positioned",
      "No console errors",
      "No TypeScript errors",
      "No unintended file changes"
    ]
  },

  "output_format": {
    "require_summary": true,
    "summary_fields": [
      "files_changed",
      "changes_made",
      "components_removed",
      "log_effect_summary_contents",
      "remaining_edge_cases"
    ]
  }
}